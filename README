TENER EN CUENTA:
agregar al servidor
-doble click en el servidor
-open launch configuration
-tab arguments
-en VM Arguments agregar un -Dspring.profiles.active=dev
esto indica que se cargara el archivo config_dev.properties


Para ejecutar los curl, "sh nombredelscript.sh" y tira los parametros que esta esperando,
se encuentran en la carpeta "scripts etapa2"

PREGUNTAS: